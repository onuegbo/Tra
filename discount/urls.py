from django.conf.urls import url
from django.conf import settings
from django.conf.urls.static import static
from .import views

app_name = 'discount'
urlpatterns = [
#    url(r'^$', views.cart_detail, name='cart_detail'),
#    url(r'^add/(?P<ticket_id>\d+)/$', views.cart_add, name='cart_add'),
#    url(r'^remove/(?P<ticket_id>\d+)/$', views.cart_remove, name='cart_remove'),

   
]